const imgContainer = document.querySelector('.img-container');
const infoContainer = document.querySelector('.info-container');
const searchBtn = document.querySelector('button');
const searchBar = document.querySelector('input')
const showName = document.createElement('div');
const showGenres = document.createElement('div')
const showStatus = document.createElement('div');
const showRating = document.createElement('div');
const showNetwork = document.createElement('div');
const showSummary = document.createElement('div');

export {showName, showGenres, showStatus, showRating, showNetwork, showSummary, imgContainer, infoContainer, searchBtn, searchBar}